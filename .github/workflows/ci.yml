name: CI

on:
  workflow_dispatch:

jobs:
  workflow-index-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke
        run: |
          set -euo pipefail
          echo "workflow index smoke ok"
